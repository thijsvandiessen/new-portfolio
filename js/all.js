$(function () {

    // check if on homepage
    if ( window.location.pathname == 'index.php' ){
        // Index (home) page
    } 

    // local check if on homepage
    else if (window.location.pathname == '/~thijsvandiessen/portfolio/') {
        // Other page
        
    }

    else {
       // on other page
    }

});